apiVersion: v1
kind: ConfigMap
metadata:
  name: flomesh-demo-configmap
  labels:
    app: flomesh-demo
data:
  # 配置文件内容
  application.yaml: |
    server:
      port: 8080
    spring:
      application:
        name: flomesh-demo
      cloud:
        nacos:
          discovery:
            server-addr: nacos.flomesh-test:8848
            metadata:
              organization: iflorens-test
          config:
            enabled: false
            refresh-enabled: false
    logging:
      level:
        org:
          springframework: DEBUG
    app:
      name: flomesh-demo-test-cm-hk